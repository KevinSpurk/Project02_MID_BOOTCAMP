TO DO:

General

- Function description: inputs, ...
- pickle implementation
- ceate/check inverse transformations
- check log transformation
- Make all functions work for multiclass reg classification too

1 Executive summary

5

- TO DO: function to select encoding 
- TO DO: replace oultiers with random numbers that strenthen the normal distribution

6
balanced classification
# make function more modular with nested functions cause large parts of the code are reused in following classification functions
# for now, adding roc to report only works binary classifications with classes 'no' and 'yes' where 'no' is maj. class. 
# Plot_binclass_roc() uses get_dummies() to dummify the target var. which outputs the dummified columns in alphabetical order (target_col_No, target_co_Yes).
# The assignment of report_balanced['AUC'] = [auc_balanced[1], auc_balanced[0],...] is not dynamic. Since the report table lists the row for maj. class always
# above min. class, this can produce the error of assigning the wrong AUC value to the classes when 'yes' is maj. class. FIX!(REGEX)

classifier comparison
# add classes separated view
add new func for bar charts for kpis with selection options

7

- Threshold parameter tuning